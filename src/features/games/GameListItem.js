import { Button } from "reactstrap"

export const GameListItem = () => {
    return (
        <>
            <div>Game info will go here</div>
            <Button type='submit' color='danger'>This button will delete the game</Button>
        </>
    )
}